# compiled output
/dist
/node_modules
/build
.pnpm-store

# secrets & local overrides (secure by default — never commit)
.env
.env.*
*.local

# foreman orchestration ledger/scratch (never commit)
.foreman/

# generator manifest consumed by .husky/pre-commit (regenerated every run)
.generated-paths
